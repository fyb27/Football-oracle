# Frontend build configuration. The app is fully client-side — it bundles the
# historical dataset and runs the prediction engine in the browser, so there is
# NO backend and no API URL to configure.

# Base public path. "/football-oracle/" for GitHub Pages project sites,
# "/" for a custom domain or local preview.
VITE_BASE=/football-oracle/
