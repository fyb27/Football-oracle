# Backend configuration (copy to .env for local overrides).
PORT=4000
# Comma-separated allowed origins, or * for any (default).
CORS_ORIGIN=*
# SQLite file path. Use an absolute path or a Docker volume in production.
DB_PATH=data/football-oracle.db
# Re-seed automatically when the DB is empty.
AUTO_SEED=true
