# Serves the static app locally in a container.
# Usage: docker compose up --build  →  http://localhost:8080
# (The app is fully client-side — there is no backend service.)
services:
  web:
    build:
      context: ./frontend
      args:
        VITE_BASE: "/"
    ports:
      - "8080:80"
    restart: unless-stopped
